import React from 'react';

const Header = () => (
	<header className="header-layout header">
		<h1>Quizzit</h1>
		<nav>
			<a>Sign Up</a>
			<a>Login</a>
			<a>Leaderboards</a>
		</nav>
	</header>
);

export default Header;